# FnDepot 飞牛NAS应用仓库

> 收录自研和收集的飞牛NAS应用插件

[![Platform](https://img.shields.io/badge/platform-FNOS-blue)](https://www.fnnas.com/)
[![License](https://img.shields.io/badge/license-MIT-yellow)](LICENSE)

## 应用列表

| 应用 | 版本 | 说明 |
|------|------|------|
| [日志哨兵](fnlogpush/) | v0.9.9 | 日志监控 + 多渠道推送 + 事件管理 |
| [USB自动同步](usbrsync/) | v0.1.7 | USB存储设备自动同步工具 |
| [清理精灵](fnclearup/) | v0.5.6 | 扫描FnOS所有vol目录，找出已卸载应用（含关联系统用户）、已删除网盘挂载、已删除docker残余卷的残留目录，一键清理 |

## 安装说明

### 日志哨兵 (fnlogpush)

飞牛NAS日志监控与多渠道推送工具

**下载地址**: [fnlogpush.fpk](fnlogpush/fnlogpush.fpk)

**功能**：
- 实时监控系统日志和备份进度
- 支持企业微信、钉钉、飞书、Bark、PushPlus、MeoW等推送渠道
- Web界面管理、事件订阅、免打扰和告警聚合
- **事件管理功能** - 添加/编辑/删除自定义事件
- **事件查询** - 可查询数据库中事件的最新记录
- **5分钟无操作自动退出登录**

**界面特性**：
- 6个精选主题（暗夜紫/深色/深海蓝/清新绿/暮色橙/科技感霓虹）
- 加载动画、骨架屏、卡片悬浮动效
- 科技感霓虹主题（Cyber）
- 状态指示器脉冲发光
- 自定义滚动条、输入框增强效果

**更新日志 (v0.9.9)**：
- 同步至最新代码版本

**更新日志 (v0.9.5)**：
- 丰富推送信息：相册照片数量、相册名称、备份路径等
- 分享相册时只推送分享消息，不推送新建相册消息
- 点击备份卡片保存按钮时自动启动/停止备份监控
- 系统状态卡片优化：数据库和监控状态同行动态
- 删除系统状态卡片的重置监控按钮

### USB自动同步 (usbrsync)

USB存储设备自动同步工具，检测到USB设备插入后自动同步指定目录。

**下载地址**: [USBRsync.fpk](usbrsync/USBRsync.fpk)

**功能**：
- 单向同步 - 从源目录同步到目标目录
- 双向同步 - 源目录和目标目录双向同步
- 插入自动同步 - 检测到USB设备插入时自动执行同步任务
- 手动同步 - 随时手动触发同步任务
- 实时显示同步进度

**界面特性**：
- 全新深色主题设计（渐变背景 + 毛玻璃效果）
- 响应式布局（自动适配桌面端和移动端）
- 流畅动画（卡片淡入、悬停变换）
- 实时日志刷新

### 清理精灵 (fnclearup)

扫描FnOS所有vol目录，找出已卸载应用（含关联系统用户）、已删除网盘挂载、已删除docker残余卷的残留目录，一键清理。

**下载地址**: [fnclearup.fpk](fnclearup/fnclearup.fpk)

**功能**：
- 自动扫描 `/mnt/vol*` 下所有 `@app*` 目录
- 与已安装应用列表对比，找出孤立目录
- 支持多选批量删除，可预览完整路径
- 直接打开即可使用，无需注册或登录

**界面特性**：
- Tab 切换：网盘挂载目录 / data/vol02 未挂载目录独立展示
- KPI 卡片：卷总数、已挂载数量、未挂载数量
- 主题按钮与赞助按钮风格统一
- 响应式布局，适配桌面端和移动端
- 纯 Bash CGI 实现，轻量无依赖
- 主题按钮与赞助按钮风格统一
- 响应式布局，适配桌面端和移动端
- 纯 Bash CGI 实现，轻量无依赖

**更新日志 (v0.3.0)**：
- 重新打包：前端全面优化（无障碍修复、模态框role标注、alert阻塞修复）
- 新增：网盘Tab新增"卷总数"KPI卡片（显示/vol02子目录总数）
- 修复：do_volumes中orphan_json缺少[]包裹导致前端"not iterable"错误
- 修复：静态文件响应缺少Status:200 OK导致浏览器解析失败
- 修复：api.sh第524/544行复杂pattern substitution导致bash -n报语法错误
- 优化：Docker残余卷批量获取driver信息，O(1)查表替代逐个inspect
- 优化：网盘挂载变量名修正（binds_json→mounts_json）

**更新日志 (v0.2.4)**：
- **修复**: 用 `docker inspect` 精确判断卷是否被容器引用，移除不可靠的 `used_file` grep 逻辑

**更新日志 (v0.2.3)**：
- 新增 Docker 卷管理功能（查看在用卷/残余卷，一键删除）

**更新日志 (v0.2.9)**：
- **修复**: 改用 `docker inspect` 单次调用获取所有容器卷信息，正确保留容器身份用于关联

**更新日志 (v0.2.7)**：
- **修复**: Docker 卷提取改用 jq select(Type==volume)，确保正确识别在用卷

**更新日志 (v0.2.6)**：
- **优化**: Docker 卷判断逻辑，大幅减少 docker inspect 调用次数，提升加载速度
- **修复**: 网盘在用列表默认收缩，移除加载后自动展开

**更新日志 (v0.2.3)**：
- 修复 api.sh 文件损坏问题，恢复完整代码

**更新日志 (v0.2.2)**：
- 修复 Docker 卷 API 路径，优化 jq 依赖

**更新日志 (v0.2.1)**：
- 新增 Docker 残余卷 Tab，支持查看/删除未使用卷

**更新日志 (v0.2.0)**：
- 新增 Tab 切换：网盘挂载目录 / data/vol02 未挂载目录独立展示
- 新增 KPI 卡片：显示已挂载数量和未挂载数量
- 主题按钮样式优化：与赞助按钮风格统一
- CSS 修复：`html.dark` 选择器、重复残缺样式块清理
- JS 修复：App 对象重复属性、`switchTab` 添加 `async` 关键字

**更新日志 (v0.1.5)**：
- 前端审计修复: XSS防护(innerHTML转义)、label for属性、WCAG AA对比度优化

## 作者

- 作者: 再见一零一二
- Gitee: https://gitee.com/wyf1015
- Email: 64652178@qq.com

## 许可证

MIT License
